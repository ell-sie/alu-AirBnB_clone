# alu-AirBnB_clone

The AirBnB console project - for setup
The project is a command interpreter for the AirBnB clone. The command interpreter allows users to interact with the system through a command-line interface and perform various actions such as creating and managing objects, retrieving information, and performing operations on the data.
